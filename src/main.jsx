import React from "react";
import ReactDOM from "react-dom/client";
import AnimeFlix from "../app/AnimeFlix.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AnimeFlix />
  </React.StrictMode>
);
